const Conteudo = ({ onClick, src, width, height, alt }) => {
  return (
    <div class="content">
      {/*            <!-- Conteúdo principal -->
            <!-- Seu conteúdo principal aqui --> */}
    </div>
  );
};

export default Conteudo;
